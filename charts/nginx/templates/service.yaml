---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "app.name" . }}  
  namespace: {{ .Values.namespace }}
spec:
  ports:
    - name: http
      port: {{ .Values.service.port }}
      protocol: TCP
      targetPort: 80
  selector:
    app: {{ include "app.name" . }}